# Contributing to Interr

**Hello there! 👋**

We're thrilled that you're eager to contribute to **Interr!** ❤️

Your interest in improving this platform means a lot. Please read through this guide so your contributions align with the project goals and standards.

### 📖 Getting Started

1. Fork the repository and clone it locally.
2. Install dependencies: `npm install`
3. Start the dev server: `npx webpack serve --mode development`
4. Make your changes on a feature branch.
5. Submit a pull request with a clear description of what you changed and why.

### 💬 Code Standards

Before submitting, make sure your code passes all checks:

```bash
npm run lint        # Check for lint issues
npm run lint-fix    # Auto-fix lint issues
npm run tsc:ci      # TypeScript type checks
```

All checks must pass with **0 warnings** before a pull request can be merged.

### ❗ Commit Format

Follow [Conventional Commits](https://www.conventionalcommits.org) with a scope:

```
feat(feature-name): description
fix(feature-name): description
docs(section): description
```

**Happy coding!**
